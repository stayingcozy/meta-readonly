#!/bin/sh
# Placeholder. Milestone 2 replaces this with a vsock listener that:
# - accepts a host connection on fixed port
# - allocates a PTY and execs the requested command
# - streams stdio back over vsock
echo "readonly-supervisor placeholder - vsock not wired yet"
